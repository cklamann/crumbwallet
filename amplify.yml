version: 1.0
frontend:
  phases:
    preBuild:
      commands:
        - cd app/client
        - npm install
    build:
      commands:
        - npm run development
  artifacts:
        baseDirectory: dist/
    discard-paths: yes