version: 1.0
frontend:
  phases:
    preBuild:
      commands:
        - cd app/client
        - npm install
    build:
      commands:
        - npm run development
  artifacts:
    baseDirectory: app/client/dist
    discard-paths: yes